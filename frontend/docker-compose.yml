version: "3.8"

services:
  app:
    build: .
    container_name: frontend
    ports:
      - "0.0.0.0:5173:5173"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - CHOKIDAR_USEPOLLING=true
